import { StyleSheet, Dimensions } from 'react-native';

export const styles = StyleSheet.create({
    fondo: {
        flex: 1,
    },
    score: {
        position: 'absolute',
        top: 50,
        left: 20,
        color: 'white',
        fontSize: 20,
        fontWeight: 'bold',
        zIndex: 10,
    },
});
